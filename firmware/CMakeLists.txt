# Top-level ESP-IDF project file. ESP-IDF's build system reads this,
# discovers components under main/ and components/, and produces a .bin.
cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(fleetos)
