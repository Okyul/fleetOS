# ESP-IDF build artifacts
build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock
*.elf
*.map

# Secrets live in sdkconfig (gitignored above) via Kconfig.projbuild prompts,
# and in .env for the host CLI. sdkconfig.defaults is committed — it holds
# only shareable build defaults (flash size, log levels), never credentials.
.env
secrets/

# Built firmware binaries — uploaded to R2, not committed
firmware-builds/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code
.claude/
